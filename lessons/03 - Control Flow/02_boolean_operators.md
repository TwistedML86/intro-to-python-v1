# Boolean Operators

> **Source:** Ported from Python Essentials — PE 1.3 (Operators — Logical Operators)
>
> **Porting notes:** PE 1.3 covers `and`, `or`, `not` briefly under the operators section. Expand this into a standalone section: truth tables, combining conditions in `if` statements, operator precedence, truthy/falsy values (0, empty string, None, empty list evaluate as False). This expansion is warranted because students in this course have less prior exposure to boolean logic than those coming from ITP.

TBD
