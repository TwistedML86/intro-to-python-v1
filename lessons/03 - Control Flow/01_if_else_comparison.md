# if/else and Comparison Operators

> **Source:** Ported from Python Essentials — PE 1.5 (Control Flow — Conditional Statements) and PE 1.4 (Block Structure and Indentation)
>
> **Porting notes:** Take PE 1.5 conditional statements section (`if`, `elif`, `else`, nested conditionals) and PE 1.4 indentation rules. These are foundational — bring them in largely as-is but expand the examples to be more beginner-friendly and interactive (use `input()` so students can actually run the examples). Comparison operators (`==`, `!=`, `<`, `>`, `<=`, `>=`) were in PE 1.3; include a brief recap here in context.

TBD
