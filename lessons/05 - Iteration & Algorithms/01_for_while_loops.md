# for and while Loops

> **Source:** Ported from Python Essentials — PE 1.5 (Control Flow — Loops)
>
> **Porting notes:** Take the loops section of PE 1.5 (`for` with `range()` and over sequences; `while`; `break`; `continue`). Expand `range()` coverage (start, stop, step). Add more worked examples that connect to things students already know (e.g., looping over a list of names collected with `input()`). The PE 1.5 "Long Lines" note can be omitted.

TBD
