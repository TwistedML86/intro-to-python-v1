# Variable Scope

> **Source:** Ported from Python Essentials — PE 1.6 (Variable Scope)
>
> **Porting notes:** Take PE 1.6 scope content largely as-is. Cover: local vs. global variables, what happens when a function defines a variable with the same name as a global, why functions can read globals but not (easily) write them, why local scope is a feature not a bug. The PE examples are good; keep them. Emphasize that scope bugs are common and show how to read the error message when a variable isn't defined where expected.

TBD
