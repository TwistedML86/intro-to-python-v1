# Defining Functions: Parameters and Return Values

> **Source:** Ported from Python Essentials — PE 1.6 (Functions — Defining and Calling Functions, Parameters and Arguments, Return Values, Default Parameters)
>
> **Porting notes:** Take PE 1.6 up through default parameters. Include: `def` syntax, calling a function, positional parameters, `return`, using return values in expressions, default parameter values. Use the same examples from PE or adapt them. **Leave out:** `*args` and `**kwargs` — these stay in Python Essentials where they connect to Dash callback patterns (noted in the landing page). Consider adding a "why functions?" motivating example before diving into syntax.

TBD
