# try/except and Common Runtime Errors

> **Source:** Ported from Python Essentials — PE 1.8 (Error Handling)
>
> **Porting notes:** Take PE 1.8 largely as-is. Cover `try`, `except`, `else`, `finally`; catching specific exceptions vs. bare `except`; raising exceptions with `raise`; the `as e` syntax for inspecting the error message. Also bring in the `logging` module from PE 1.7 here (it was intentionally held back from Week 1) — students are now writing multi-function programs where logging is more meaningful than `print`. Add a table of common runtime exceptions students will actually encounter: `ValueError`, `TypeError`, `KeyError`, `IndexError`, `FileNotFoundError`, `ZeroDivisionError`.

TBD
