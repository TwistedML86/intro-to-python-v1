# Running Python Scripts from the Command Line

> **Source:** New content
>
> **Notes:** Students have already written Python in VS Code (from 02_01). Now connect that to the terminal. Cover: navigating to the project folder in the terminal; running `python script.py` (or `python3`); reading stdout output in the terminal; what happens when there's a syntax error; the assignment submission workflow going forward (paste terminal output). This is a short, practical section — keep it tight.

TBD
