# Dictionaries and Sets

> **Source:** Ported from Python Essentials — PE 2.1 (Dictionaries and Sets sections)
>
> **Porting notes:** Take PE 2.1 dictionaries and sets content. Include: creating dicts, accessing by key, adding/updating key-value pairs, key dictionary methods (`keys`, `values`, `items`, `get`), the review table comparing all four structures. Sets: creating, `add`, and the three common set operations (union, intersection, difference). Add a "when would you use this?" framing for each structure that PE doesn't explicitly spell out.

TBD
